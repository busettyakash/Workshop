name: Production — Trivy Scan & Deploy

on:
  workflow_dispatch:

jobs:
  # ─────────────────────────────────────────
  # FRONTEND — Build & Scan
  # ─────────────────────────────────────────
  trivy-scan-frontend:
    name: Frontend — Build & Trivy Scan
    runs-on: ubuntu-latest
    outputs:
      scan_status: ${{ steps.trivy.outputs.scan_status }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Frontend Docker image
        run: docker build -t frontend-prod:${{ github.sha }} .

      - name: Install Trivy
        run: |
          sudo apt-get install -y wget apt-transport-https gnupg lsb-release
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" \
            | sudo tee -a /etc/apt/sources.list.d/trivy.list
          sudo apt-get update && sudo apt-get install -y trivy

      - name: Run Trivy scan — Frontend
        id: trivy
        run: |
          trivy image \
            --exit-code 1 \
            --severity MEDIUM,HIGH,CRITICAL \
            --format table \
            --output trivy-frontend-report.txt \
            frontend-prod:${{ github.sha }}
          echo "scan_status=passed" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Generate JSON report — Frontend
        run: |
          trivy image \
            --exit-code 0 \
            --severity MEDIUM,HIGH,CRITICAL \
            --format json \
            --output trivy-frontend-report.json \
            frontend-prod:${{ github.sha }}

      - name: Upload Frontend Trivy report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-prod-frontend-${{ github.sha }}
          path: |
            trivy-frontend-report.txt
            trivy-frontend-report.json
          retention-days: 15

      - name: Post frontend scan summary
        if: always()
        run: |
          echo "## Trivy Scan — Frontend (Production)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat trivy-frontend-report.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Block if frontend vulnerabilities found
        if: steps.trivy.outputs.scan_status != 'passed'
        run: |
          echo "❌ Frontend vulnerabilities found. Production deploy blocked."
          exit 1

      - name: Cleanup Frontend image
        if: always()
        run: docker rmi frontend-prod:${{ github.sha }} || true

  # ─────────────────────────────────────────
  # BACKEND — Build & Scan
  # ─────────────────────────────────────────
  trivy-scan-backend:
    name: Backend — Build & Trivy Scan
    runs-on: ubuntu-latest
    outputs:
      scan_status: ${{ steps.trivy.outputs.scan_status }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Backend Docker image
        run: docker build -t backend-prod:${{ github.sha }} ./backend

      - name: Install Trivy
        run: |
          sudo apt-get install -y wget apt-transport-https gnupg lsb-release
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" \
            | sudo tee -a /etc/apt/sources.list.d/trivy.list
          sudo apt-get update && sudo apt-get install -y trivy

      - name: Run Trivy scan — Backend
        id: trivy
        run: |
          trivy image \
            --exit-code 1 \
            --severity MEDIUM,HIGH,CRITICAL \
            --format table \
            --output trivy-backend-report.txt \
            backend-prod:${{ github.sha }}
          echo "scan_status=passed" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Generate JSON report — Backend
        run: |
          trivy image \
            --exit-code 0 \
            --severity MEDIUM,HIGH,CRITICAL \
            --format json \
            --output trivy-backend-report.json \
            backend-prod:${{ github.sha }}

      - name: Upload Backend Trivy report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-prod-backend-${{ github.sha }}
          path: |
            trivy-backend-report.txt
            trivy-backend-report.json
          retention-days: 15

      - name: Post backend scan summary
        if: always()
        run: |
          echo "## Trivy Scan — Backend (Production)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat trivy-backend-report.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Block if backend vulnerabilities found
        if: steps.trivy.outputs.scan_status != 'passed'
        run: |
          echo "❌ Backend vulnerabilities found. Production deploy blocked."
          exit 1

      - name: Cleanup Backend image
        if: always()
        run: docker rmi backend-prod:${{ github.sha }} || true

  # ─────────────────────────────────────────
  # DEPLOY FRONTEND — only if scan passed
  # ─────────────────────────────────────────
  deploy-frontend:
    name: Deploy Frontend → Production
    needs: trivy-scan-frontend
    runs-on: ubuntu-latest
    if: needs.trivy-scan-frontend.outputs.scan_status == 'passed'
    environment:
      name: production-frontend
      url: https://workshop-workspace.vercel.app/


    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy Frontend to Vercel (Production)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_FRONTEND_PROJECT_ID }}
        run: |
          npm install -g vercel
          vercel pull --yes --environment=production --token=$VERCEL_TOKEN
          vercel build --prod --token=$VERCEL_TOKEN
          vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN

      - name: Post frontend deploy summary
        run: |
          echo "## ✅ Frontend — Production Deployed" >> $GITHUB_STEP_SUMMARY
          echo "- Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- URL: https://your-app.vercel.app" >> $GITHUB_STEP_SUMMARY

  # ─────────────────────────────────────────
  # DEPLOY BACKEND — only if scan passed
  # ─────────────────────────────────────────
  deploy-backend:
    name: Deploy Backend → Production
    needs: trivy-scan-backend
    runs-on: ubuntu-latest
    if: needs.trivy-scan-backend.outputs.scan_status == 'passed'
    environment:
      name: production-backend
      url: https://workshop-workspace.vercel.app/

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy Backend to Vercel (Production)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_BACKEND_PROJECT_ID }}
        run: |
          npm install -g vercel
          vercel pull --yes --environment=production --token=$VERCEL_TOKEN
          vercel build --prod --token=$VERCEL_TOKEN
          vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN

      - name: Post backend deploy summary
        run: |
          echo "## ✅ Backend — Production Deployed" >> $GITHUB_STEP_SUMMARY
          echo "- Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- URL: https://your-api.vercel.app" >> $GITHUB_STEP_SUMMARY
